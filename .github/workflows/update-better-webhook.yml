name: Legacy better-webhook updater (deprecated)

on:
  workflow_dispatch:
  # Deprecated fallback.
  # better-webhook cask updates are now created by GoReleaser from
  # github.com/endalk200/better-webhook.

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Display migration note
        run: |
          echo "This workflow is deprecated."
          echo "better-webhook updates are now published as Homebrew Cask PRs"
          echo "from the better-webhook GoReleaser release pipeline."
          echo "See https://github.com/endalk200/better-webhook/actions/workflows/release-cli.yml"
